# وثيقة التوجيه المعماري للواجهة الأمامية (Frontend Architecture Guide)
## مشروع نظام تخطيط موارد المؤسسات القابل للتوسعة (Modular ERP System)

هذا المستند موجه خصيصاً لمطور الواجهة الأمامية (Frontend Developer) وأدوات الذكاء الاصطناعي المساعدة له، ليكون المرجع الأساسي لبناء واجهة النظام بشكل يضمن الاستقرار، السرعة، والقابلية العالية للتوسع. يرجى مراجعة هذا المستند جنباً إلى جنب مع الوثائق المرجعية الأخرى للمشروع (مثل وثيقة المتطلبات PRD).

---

### ١. الرؤية العامة للنظام (The Vision)
نحن نبني نظام **Modular ERP** يعتمد على معمارية النواة والإضافات (Core & Plugins).
*   **النواة (The Core):** بيئة عمل نظيفة تحتوي فقط على الأساسيات المشتركة لأي كيان تجاري (المصادقة، دليل الكيانات، الإعدادات، والمركز المالي المبسط).
*   **الإضافات (The Plugins):** وحدات مستقلة (مثل إدارة المخزون، دورة الاستيراد، مساعد واتساب الذكي الذي يفهم الرسائل الصوتية، ومؤشرات الثقة وتقييم خطورة الطلبات) يتم تفعيلها ديناميكياً بناءً على مجال عمل العميل. الواجهة الأمامية يجب أن تكون ذكية لتعرض مكونات الإضافات المفعلة فقط دون التأثير على سرعة النظام.

### ٢. حزمة التقنيات المعتمدة (Tech Stack)
يجب الالتزام بالتقنيات التالية لضمان الأداء والاستقرار:
*   **إطار العمل:** Next.js (App Router) مع React.
*   **الأنماط والتصميم:** Tailwind CSS + Shadcn UI (لبناء مكتبة مكونات متناسقة وقابلة لإعادة الاستخدام).
*   **إدارة الحالة والبيانات:**
    *   Zustand (لإدارة الحالة العامة الخفيفة مثل تفضيلات المستخدم).
    *   React Query (لإدارة جلب البيانات من الـ API، التخزين المؤقت Caching، والمزامنة).
*   **النماذج والتحقق (Forms & Validation):** React Hook Form مع Zod.
*   **اللغة الأساسية:** TypeScript (بشكل صارم Strict Mode).

### ٣. المعمارية وهيكل الملفات (Architecture & Folder Structure)
يجب تقسيم المشروع ليُسهل إدارة النواة وفصل الإضافات عنها:

```text
src/
├── app/                  # (Next.js App Router)
│   ├── (auth)/           # شاشات تسجيل الدخول والاشتراك
│   ├── (core)/           # شاشات النواة (لوحة التحكم، دليل الكيانات، الإعدادات)
│   └── (plugins)/        # مجلدات الإضافات (كل إضافة في مجلد مستقل)
│
├── components/           # (المكونات المشتركة - Reusable Components)
│   ├── ui/               # مكونات Shadcn UI (Buttons, Inputs, Dialogs)
│   ├── layout/           # الهيكل العام (Sidebar, Navbar, Layout Wrapper)
│   └── shared/           # مكونات معقدة مشتركة (DataTables, Generic Forms)
│
├── lib/                  # (الدوال والمساعدات - Utilities)
│   ├── api-client.ts     # إعدادات الاتصال بالباك إند (Axios أو Fetch)
│   ├── formatters.ts     # دوال تنسيق الأرقام، العملات، والتواريخ
│   └── utils.ts          # دوال مساعدة عامة (مثل دمج كلاسات Tailwind)
│
├── store/                # (إدارة الحالة - State Management)
│   └── use-app-store.ts  # إدارة حالة المستخدم، الثيم، والإضافات المفعلة
│
└── types/                # (تعريفات TypeScript)
    ├── core.ts           # واجهات البيانات الأساسية (User, Contact, Company)
    └── plugins.ts        # واجهات بيانات الإضافات المشتركة
```

### ٤. التعليمات الأساسية لأداة الذكاء الاصطناعي (AI Instructions)
عند كتابة كود الواجهة الأمامية، يرجى الالتزام بالضوابط التالية:

#### أ. التحميل الديناميكي (Lazy Loading)
يجب عدم تحميل مكونات الإضافات الثقيلة أو الشاشات غير المخصصة للنواة في الحزمة المبدئية (Initial Bundle). استخدم `next/dynamic` لتحميل المكونات عند الحاجة فقط.

#### ب. حدود الأخطاء (Error Boundaries)
لتجنب تحول النظام بأكمله إلى "شاشة بيضاء" بسبب خطأ في إضافة واحدة، يجب تغليف كل قسم رئيسي (Route) وكل مكون إضافة (Plugin Component) بـ Error Boundary. إذا فشل تحميل إضافة "الشحن"، تظهر رسالة خطأ موضعية، ويستمر النظام الأساسي في العمل.

#### ج. مكتبة المكونات (Component Reusability)
لا تقم ببناء جدول (Table) أو نموذج (Form) من الصفر لكل شاشة. يجب بناء مكون `<DataTable />` ديناميكي يستقبل البيانات، الأعمدة، وإعدادات الترتيب والبحث كـ Props. الأمر ذاته ينطبق على نماذج الإدخال.

#### د. التحقق الصارم (Strict Validation)
كل مدخلات المستخدم يجب أن تمر عبر مخطط Zod (Schema) متصل بـ React Hook Form قبل إرسالها للباك إند. لا تعتمد مطلقاً على التحقق الافتراضي للمتصفح.

#### هـ. الواجهة الديناميكية بناءً على الإضافات (Feature Flags)
قم ببناء سياق (Context) أو استخدام Zustand للاحتفاظ بقائمة الـ `active_plugins` الخاصة بالعميل الحالي. القائمة الجانبية (Sidebar) يجب أن تقرأ من هذه القائمة لتقرر أي أزرار أو روابط ستعرضها.

### ٥. المخرجات المتوقعة في المرحلة الأولى
1.  تأسيس هيكل المشروع كما هو موضح أعلاه.
2.  بناء الـ Layout الأساسي (القائمة الجانبية الديناميكية، وشريط التنقل العلوي).
3.  بناء مكون الـ DataTable الأساسي الذي سيدعم النظام بأكمله.
4.  بناء شاشة "دليل الكيانات" (Contacts Directory) كأول تطبيق عملي للنواة.
