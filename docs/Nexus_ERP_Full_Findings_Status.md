# حالة الـ68 ملاحظة بالكامل — نظام Nexus ERP

تاريخ التوثيق: 7 سبتمبر 2026

**الغرض:** يغطي هذا الملف كل ملاحظة من الـ68 ملاحظة الموجودة في `docs/Nexus_ERP_Audit_Report.md` (12 حرج + 15 عالي + 25 متوسط + 16 منخفض) واحدة واحدة، بحالتها الفعلية الحقيقية في الكود دلوقتي — **مش الحالة وقت كتابة التقرير الأصلي (17 أغسطس 2026)**. كل ملاحظة هنا اتفحصت مباشرة في الكود (grep/قراءة فعلية)، مش تخمين.

**الرموز المستخدمة:**
- ✅ **تم بالكامل** — الفجوة اتقفلت فعليًا، متحقق منها في الكود.
- ⚠️ **جزئي** — جزء من المشكلة اتحل، جزء لسه موجود. التفاصيل موضّحة.
- ❌ **لسه موجودة** — الملاحظة الأصلية لسه صحيحة زي ما هي، اتأكدت مرة تانية.
- ❓ **غير مؤكد** — معملناش تحقق كافي عشان نجزم.

**ملحوظة مهمة:** هذا الملف بديل أدق من `docs/Nexus_ERP_Handoff_Status.md` (اللي كان بيغطي بس قائمة الأولويات المُلخّصة، 35 بند). استخدم الملف ده لو محتاج تفاصيل كل ملاحظة من الـ68 بالظبط.

---

## القسم 1: الأمان وحسابات المستخدمين

**1. استعادة كلمة المرور غير موجودة نهائيًا `[حرج]`**
✅ **تم بالكامل.** `POST /forgot-password` و `/reset-password` في `backend/app/modules/system/router.py`، مع صفحات فرونت إند كاملة `app/(auth)/forgot-password/page.tsx` و `reset-password/page.tsx`.

**2. لا يوجد تدوير (rotation) لرمز التحديث (refresh token) `[عالي]`**
✅ **تم بالكامل.** `security.py` فيه `rotate_refresh_token` + `detect_refresh_token_reuse` (كشف سرقة الرمز)، مربوطين في `system/router.py::refresh_token`.

**3. لا توجد مصادقة ثنائية (2FA) للمستخدمين الداخليين `[متوسط]`**
❌ **لسه موجودة.** لا يوجد أي كود TOTP/MFA في الباك إند كله.

**4. خانة "تذكرني" غير فعّالة `[منخفض]`**
✅ **تم بالكامل (تبين إنها شغالة فعلاً).** `login/page.tsx` بتمرر `persist` لـ `saveSession()`، واللي بيتحكم فعليًا في التخزين (`localStorage` لو مفعّلة، `sessionStorage` لو لأ) عبر `api-client.ts::writeKey`. الملاحظة الأصلية غير دقيقة أو كانت صحيحة وقتها وبعدين اتصلحت.

**5. الصلاحيات (RBAC) — أكبر فجوة أمان `[حرج]`**
⚠️ **جزئي — أهم فجوة متبقية في كل التقرير.** `require_role`/`require_permission` مستخدمة في ~4 أماكن بس (`hr/api/leave_requests.py`, `accounting/router.py`) من أصل 43 موديول API. الواجهة الأمامية بتعرض الدور بس، مفيش منع فعلي لعناصر الواجهة حسب الصلاحية.

**6. القائمة الجانبية لا تُفلتر حسب القطاعات المفعّلة `[عالي]`**
✅ **تم بالكامل.** `frontend/src/app/dashboard/layout.tsx` بيجيب `/plugins` ويفلتر عناصر القائمة فعليًا حسب القطاعات المفعّلة لكل مستأجر.

---

## القسم 2: المحاسبة والمالية

**7. لا توجد تقارير مالية متاحة في الواجهة `[حرج]`**
⚠️ **جزئي.** ميزان المراجعة (Trial Balance) موجود فعليًا في `dashboard/accounting/page.tsx` (`/accounting/reports/trial-balance`). **قائمة الدخل والميزانية العمومية لسه غير موجودتين لا في الباك إند ولا الفرونت إند.**

**8. زر إرسال الفاتورة الإلكترونية (ETA) غير موجود `[حرج]`**
✅ **تم بالكامل.** موجود في `dashboard/sales/page.tsx` (`submitEtaMutation` → `POST /eta/submissions/invoice/{id}`)، والباك إند كامل.

**9. ارتداد الشيك لا يُحدث أي أثر مالي أو تنبيه `[عالي]`**
✅ **تم بالكامل (تبين إنها شغالة فعلاً).** `finance/services/cheque_service.py::bounce_cheque()` بينشئ قيد محاسبي عكسي حقيقي (يعكس المدين/الدائن)، مش مجرد تغيير حالة شكلي.

---

## القسم 3: المبيعات والمشتريات والمخزون

**10. لا يوجد "أمر بيع" (Sales Order) — الفاتورة تُنشأ مباشرة `[متوسط]`**
⚠️ **جزئي.** موديل `SalesOrder` حقيقي موجود في الباك إند (`sales/models/core.py`, `sales/api/orders.py`)، لكن **`dashboard/sales/page.tsx` نفسها موثّقة صراحة (تعليق في الكود) إنها بتستخدم مسار الفوترة المباشرة "بدون أمر بيع سابق"** — يعني الفرونت إند لسه بيتجاهل الـ Sales Order الموجود في الباك إند.

**11. لا توجد واجهة لتسجيل دفعات على الفواتير، ولا مرتجعات مبيعات `[عالي]`**
✅ **تم بالكامل.** واجهة تحصيل الدفعات موجودة (زرار "تحصيل دفعة" في صفحة المبيعات)، وواجهة المرتجعات موجودة كمان (زرار "مرتجع" + modal مخصص لإصدار إشعار دائن).

**12. لا يوجد أمر شراء (PO) ولا إذن استلام بضاعة — three-way match شكلي `[عالي]`**
⚠️ **جزئي.** منطق المطابقة الثلاثية (`purchasing/services/billing.py::execute_three_way_match`) **حقيقي فعليًا** (0% تسامح، مش شكلي). لكن **`dashboard/purchases/page.tsx` موثّقة صراحة إنه لا توجد واجهة لإنشاء أمر شراء أو إذن استلام** — يعني الفواتير المُنشأة من هذه الشاشة غير مربوطة بأي PO/GRN فعليًا، فالمطابقة بتنجح تلقائيًا بشكل غير مفيد عمليًا.

**13. لا يوجد تقرير تقييم مخزون، ولا اقتراح إعادة طلب، ولا جرد فعلي `[متوسط]`**
❌ **لسه موجودة.** الباك إند فيه `inventory/reports/reorder.py` و `inventory/models/reorder.py`، لكن **لا توجد أي واجهة أمامية** لأي من هذه الميزات — `dashboard/inventory/page.tsx` فيها بس تبويبي "الأصناف" و"المخازن".

**14. لا توجد واجهة لتحويلات المخزون بين الفروع/المخازن `[منخفض]`**
❌ **لسه موجودة.** endpoint الباك إند موجود (`inventory/api/transfer.py`)، لكن مفيش أي صفحة/تبويب في الفرونت إند يستخدمه.

---

## القسم 4: نقطة البيع (POS)

**15. لا يوجد استرجاع/مرتجع من نقطة البيع `[عالي]`**
✅ **تم بالكامل.** `dashboard/pos/page.tsx` فيها `RefundSaleModal` وحقل `is_refund` فعلي.

**16. طريقة الدفع في POS مثبّتة على "نقدي" فقط `[متوسط]`**
❌ **لسه موجودة.** الباك إند (`pos/api.py`) بيقبل أي قيمة لـ `payment_method`، لكن الفرونت إند مفيهوش أي واجهة اختيار طريقة دفع — بترسل قيمة ثابتة فعليًا.

**17. لا توجد حاسبة للباقي ولا دعم لقارئ الباركود `[منخفض]`**
❌ **لسه موجودة.** لا يوجد أي منطق "الباقي" أو مسح باركود في `pos/page.tsx`.

---

## القسم 5: جهات الاتصال، الموردون، شبكة الثقة

**18. كود ميت وخطر في وحدة جهات الاتصال `[حرج]`**
✅ **تم بالكامل.** تم فحص `contacts/service.py` و`router.py` و`models.py` و`schemas.py` بالكامل — الكود نضيف، مفيش أي أنماط خطرة أو TODO معلّق.

**19. مفهومان منفصلان لـ"المورد" بدون ربط بينهما `[عالي]`**
❌ **لسه موجودة.** `PurchaseOrder.supplier_id` و`GoodsReceipt.supplier_id` حقول UUID عادية **بدون foreign key** فعلي لجدول `contacts.Contact` (بعكس حقول تانية زي `po_id`/`grn_id` اللي ليها foreign key حقيقي).

**20. لا يوجد فحص تكرار عند إنشاء جهة اتصال جديدة `[متوسط]`**
✅ **تم بالكامل (تبين إنها شغالة فعلاً).** `contacts/router.py::create_contact()` فيها فحص تكرار حقيقي على الهاتف/الإيميل بيرجع 409 Conflict.

**21. شبكة الثقة: السر الخاص بالتشفير placeholder، ومنع تكرار الاستعلام غير مُفعّل `[متوسط]`**
⚠️ **جزئي.** جزء "السر placeholder" **اتصلح فعليًا** — `config.py::_guard_production_misconfiguration()` بيرفض تشغيل النظام في الإنتاج لو `TRUST_PEPPER` لسه على القيمة الافتراضية. لكن **منع تكرار الاستعلام (rate limiting) لسه مجرد stub فارغ** في `trust/services/gates.py` (تعليق بيقول "Redis rate limit هيتولى الموضوع" بدون أي كود فعلي).

---

## القسم 6: الشحن والتسويات والاستيراد

**22. مصاريف الاستيراد لا تصل أبدًا لتكلفة المخزون `[حرج]`**
✅ **تم بالكامل.** `imports/services/landed_cost.py::apply_landed_costs` بيكتب فعليًا في `DossierCostLayer`.

**23. التسويات (Settlements): منطق قوي بالباك إند لكن بدون أي واجهة `[عالي]`**
✅ **تم بالكامل.** `frontend/src/app/dashboard/settlements/page.tsx` (617 سطر) — واجهة كاملة لرفع/مطابقة/اعتماد كشوف الحسابات، مربوطة بـ `/finance/settlements/*`.

**24. لا توجد إدارة مستندات في وحدة الاستيراد `[متوسط]`**
❌ **لسه موجودة.** لا يوجد أي كود متعلق بمستندات/مرفقات في `backend/app/modules/imports` كله.

---

## القسم 7: الموارد البشرية، فريق العمل، الحالات (Cases)

**25. لا يوجد ربط بين حساب "فريق العمل" وسجل "الموظف" في HR `[عالي]`**
❌ **لسه موجودة.** موديل `Employee` (`hr/models/core.py`) مفيهوش أي حقل `user_id` أو foreign key لجدول المستخدمين.

**26. لا توجد إدارة إجازات أو حضور وانصراف `[متوسط]`**
⚠️ **جزئي.** إدارة الإجازات (Leave Requests) **موجودة فعليًا** (`hr/api/leave_requests.py`, `hr/services/leave.py`). **الحضور والانصراف (clock-in/out) لسه غير موجود نهائيًا.**

**27. صفحة "الحالات" العامة تكرر ما هو ظاهر داخل كل قطاع `[منخفض]`**
❌ **لسه موجودة (اتأكدت تاني).** صفحة `cases/page.tsx` وصفحات القطاعات (زي `travel/page.tsx`) بيجيبوا نفس البيانات بشكل منفصل بدون أي ربط أو Cross-link حقيقي بينهم.

---

## القسم 8: الموافقات، الفوترة، السوق، الأخبار

**28. محرك الموافقات غير مربوط بأي وحدة أخرى `[حرج]`**
⚠️ **جزئي.** المحرك **مربوط فعليًا بالمشتريات** (`purchasing/services/orders.py`، وسم "Wave 2" في الكود). **لكن لسه غير مربوط بأي وحدة تانية** (لا القيود المحاسبية الكبيرة، لا المبيعات، لا HR) — الملاحظة الأصلية كانت "مش مربوط بحاجة خالص"، ودلوقتي "مربوط بحاجة واحدة بس".

**29. السوق (Marketplace): تفعيل حقيقي للقطاعات لكن بدون أي بيع/ترقية `[منخفض]`**
❌ **لسه موجودة (اتأكدت تاني).** مفيش أي كود دفع/checkout/billing مرتبط بتفعيل الإضافات في `backend/app/modules/marketplace` كله.

---

## القسم 9: القطاعات الأربعة (السياحة، الضيافة، تأجير السيارات، التوظيف)

**30. السياحة: خطأ حقيقي في حساب هامش الربح `[حرج]`**
✅ **تم بالكامل (مفيش باج).** تم فحص `travel/services/financials.py` — الحساب متسق، مفيش حقل undefined.

**31. السياحة: VendorRateCard موجود لكن غير مستخدم فعليًا `[متوسط]`**
✅ **تم بالكامل (تبين إنه مستخدم فعليًا).** `cases/api/vendors.py` بيستعلم/يربط `VendorRateCard` فعليًا (بحث حسب نوع الخدمة والتاريخ).

**32. السياحة: لا يوجد رفع مستندات ولا قائمة ركاب منظمة `[متوسط]`**
⚠️ **جزئي/غير موجودة غالبًا.** فيه موديل `VisaApplication` بحقل حالة "مستندات مكتملة"، لكن **مفيش endpoint فعلي لرفع الملفات**. بيانات الركاب لسه غير منظمة (حقول نصية بسيطة موصوفة في الكود نفسه بأنها "مدخلات manifest، مش سجلات كاملة").

**33. الضيافة: لا يوجد تتبع حالة التنظيف للغرف `[متوسط]`**
❌ **لسه موجودة.** مفيش أي حقل حالة تنظيف/فحص في `backend/app/plugins/hospitality` — `rooms.py` فيه بس نوع الغرفة والدور.

**34. الضيافة: لا يوجد تقرير إشغال (Occupancy) أو RevPAR `[متوسط]`**
❌ **لسه موجودة.** مفيش أي حساب Occupancy أو RevPAR في وحدة الضيافة كلها.

**35. الضيافة: لا يوجد تسعير موسمي `[منخفض]`**
❌ **لسه موجودة.** مفيش أي إشارة لـ"موسم" في وحدة الضيافة كلها.

**36. تأجير السيارات: لا توجد جدولة صيانة `[متوسط]`**
❌ **لسه موجودة.** مفيش أي كود متعلق بالصيانة في `backend/app/plugins/rental` (بس `fleet.py` و`inspection.py`).

**37. تأجير السيارات: أضرار الفحص عند التسليم لا تُحوَّل تلقائيًا لفاتورة `[متوسط]`**
❌ **لسه موجودة.** `services/inspection.py` بيحسب الفرق بين حالة الاستلام والتسليم (`total_penalties`) لكن **بس بيسجله كعلامة في بيانات الحالة، من غير أي إنشاء فاتورة أو رسوم فعلية.**

**38. التوظيف والاستقدام: لا يوجد تتبع للتأشيرة/تصريح العمل `[حرج]`**
✅ **تم بالكامل.** مرحلة `visa_processing` مضافة فعليًا في `recruitment/bootstrap.py` (وسم "Wave 2").

**39. التوظيف: لا توجد مرحلة مقابلة رسمية، ولا إدارة عرض عمل، ولا رفع مستندات `[عالي]`**
❌ **لسه موجودة.** مفيش أي موديل/ملف لمقابلة أو عرض عمل في `backend/app/plugins/recruitment` (بس `JobOrder`/`JobOrderCase`)، ومفيش كود رفع مستندات.

**40. التوظيف: لا يوجد نموذج بيانات مخصص للمرشح (Candidate) `[متوسط]`**
❌ **لسه موجودة.** المرشحين لسه بيانات JSON عامة جوه `Case.data` (نوع الحالة `candidate_deployment`)، مفيش موديل `Candidate` مخصص — موثّق صراحة في تعليقات `matching.py` نفسها.

---

## القسم 10: الإعدادات والـ Onboarding

**41. لا يوجد محرر لبيانات الشركة أو الإعدادات الضريبية `[متوسط]`**
❌ **لسه موجودة.** صفحة الإعدادات فيها بس تبويبي "عام" (قراءة فقط من `/auth/me`) و"التكاملات" — مفيش محرر بيانات شركة أو ضرائب.

**42. Onboarding خطوة واحدة فقط `[منخفض]`**
❌ **لسه موجودة (اتأكدت تاني).** `onboarding/systems/page.tsx` لسه شاشة واحدة بس (اختيار/تجربة القطاعات) وبعدها تحويل مباشر للداشبورد.

---

## القسم 11: بوابة العملاء، الإشعارات، عناصر الهيدر

**43. بوابة العملاء: باك إند كامل بدون أي واجهة `[عالي]`**
✅ **تم بالكامل.** الفرونت إند بالكامل اتبنى في Wave 3 بند 5 (تسجيل دخول OTP، عرض الفواتير، الدفع) — واتاختبر حي.

**44. لا يوجد نظام إشعارات داخل التطبيق — الجرس شكلي بالكامل `[متوسط]`**
⚠️ **جزئي.** الجرس **مش شكلي فعليًا** — بيعرض تنبيهات انتهاء صلاحية حقيقية (`/cases/alerts/expirations`) في قائمة منسدلة فعلية شغالة. **لكنه مش نظام إشعارات عام** — بيغطي بس تنبيهات انتهاء الصلاحية، مش أحداث تانية زي الفواتير المتأخرة أو الموافقات المعلّقة.

**45. شريط البحث في الهيدر شكلي بالكامل `[منخفض]`**
❌ **لسه موجودة (اتأكدت تاني).** حقل `searchValue` في `layout.tsx` بيتحدث بس عند الكتابة، **مفيش أي submit أو بحث فعلي يستخدمه في أي مكان تاني بالكود.**

**46. لا توجد قائمة منسدلة فعلية للصورة الشخصية `[منخفض]`**
✅ **تم بالكامل (تبين إنها شغالة فعلاً).** قائمة منسدلة حقيقية (`profileOpen` state، `role="menu"`، إغلاق بالـ Escape وبالنقر بره، فيها زرار تسجيل خروج فعلي).

---

## القسم 12: البنية التحتية — الاختبارات، CI/CD، الجاهزية للإنتاج

**47. اختبار التزامن الكبير (50 ألف عملية) لا يعمل في أي مكان `[متوسط]`**
❓ **غير مؤكد بالكامل، لكن الأرجح لسه موجودة.** الملف موجود فعليًا (`tests/load/test_50k_costing_concurrency.py`) لكن **مش موصول بـ CI** (`.github/workflows/ci.yml`)، ومفيش دليل على وجود عملية تشغيل يدوي موثّقة له.

**48. لا توجد خطوة CI للفرونت إند إطلاقًا `[متوسط]`**
❌ **لسه موجودة.** `ci.yml` فيه بس jobs مخصصة لـ `backend/` — مفيش أي job يلمس `frontend/`.

**49. أخطاء mypy لا تمنع نجاح الـ CI `[منخفض]`**
❌ **لسه موجودة.** `continue-on-error: true` لسه موجود فعليًا فوق أمر `mypy .` في `ci.yml`.

**50. app/core/audit/ كود مهجور `[متوسط]`**
❌ **لسه موجودة.** مفيش أي استيراد لـ `app.core.audit` في أي مكان تاني بالباك إند كله — لسه مهجور فعليًا.

**51. DEBUG=true بدون حماية من التشغيل بهذا الوضع في الإنتاج `[متوسط]`**
✅ **تم بالكامل.** `config.py::validate_environment` بيرفض تشغيل النظام (يرمي `ValueError`) لو `DEBUG=true` و`ENVIRONMENT=production` مع بعض.

**52. خطر توافق في إصدارات الاعتماديات بالفرونت إند `[منخفض]`**
❌ **لسه موجودة.** `package.json` فيه `next: ^16.2.10` مع `react: ^18` — لكن Next.js 16 محتاج React 19+ فعليًا. عدم توافق حقيقي.

---

## القسم 13: أمان تقني إضافي

**53. تسجيل دخول بوابة العملاء بدون أي تحقق حقيقي من الهوية `[حرج]`**
✅ **تم بالكامل.** OTP حقيقي backed بـ Redis (bcrypt hash، صلاحية 5 دقايق، حد أقصى 5 محاولات).

**54. الـ webhooks تقبل بيانات مزوّرة لو غاب التوقيع الأمني `[حرج]`**
✅ **تم بالكامل.** Paymob (HMAC-SHA512)، WhatsApp (X-Hub-Signature-256)، والشحن — كلهم برفضوا (403/400) بدون توقيع صحيح.

**55. بناء استعلام SQL يدوي في مسار واحد فقط `[منخفض]`**
⚠️ **جزئي — أوسع مما ذكره التقرير الأصلي.** فيه أكتر من مسار واحد فعليًا: `session.execute(f"SET search_path TO {tenant_schema}")` بدون `text()` موجود في أكتر من ملف `listeners.py` (rental, hospitality, recruitment, travel) — مش بس مسار واحد زي ما التقرير قال.

---

## القسم 14: قاعدة البيانات والجاهزية للإنتاج

**56. سكريبت استعادة النسخة الاحتياطية محاكاة فقط `[حرج]`**
✅ **تم بالكامل (تبين إنها شغالة فعلاً).** تم التحقق مباشرة من `tenant_restore.py` — `pg_restore` حقيقي، مش محاكاة. هذه الملاحظة كانت غير دقيقة وقت كتابة هذا التوثيق (اتصلحت في مكان ما بين تاريخ التقرير وحالا).

**57. لا يوجد أي استراتيجية نسخ احتياطي موثّقة أو مجدولة `[عالي]`**
✅ **تم بالكامل.** موثّقة بالكامل في `docs/Nexus_ERP_Backup_Strategy.md` (Wave 3 بند 4).

**58. لا يوجد حماية من التكرار عند الدفع/الفوترة `[عالي]`**
⚠️ **جزئي.** موجودة على `sales/api/invoices.py`, `inventory/api/transfer.py`, `pos/api.py`, `purchasing/api/orders.py`. **غير موجودة على `sales/api/payments.py`** (تسجيل دفعات المبيعات نفسها).

**59. حذف نهائي بدون أثر لبيانات مالية في السياحة `[متوسط]`**
❌ **لسه موجودة (اتأكدت تاني).** `travel/api_visas.py::delete_visa` و `api_packages.py::delete_package` بيستخدموا حذف نهائي حقيقي (`session.delete`)، رغم إن `TravelPackage.is_active` موجود فعليًا كحقل كان ممكن يُستخدم للإلغاء بدل الحذف.

**60. لا يوجد إعداد Docker جاهز للإنتاج `[متوسط]`**
❌ **لسه موجودة.** يوجد `backend/Dockerfile.dev` بس (تطوير فقط)، مفيش Dockerfile إنتاجي حقيقي في المشروع كله.

---

## القسم 15: تجربة المستخدم، الأداء، وإتاحة الاستخدام

**61. الداشبورد الرئيسي يعرض أرقامًا ورسومًا بيانية وهمية `[عالي]`**
⚠️ **جزئي (Wave 3).** الكروت الأربعة الآن ببيانات حقيقية (عدد الطلبات، العملاء الجدد، مع trend حقيقي). الرسوم البيانية الثلاثة (إيرادات/مصروفات/فروع) اتبدلت بعلامة صريحة "غير متاح بعد" بدل أرقام وهمية — مش بيانات حقيقية فعليًا، لكن مش مضلِّلة كمان. يحتاج تصميم نموذج بيانات فروع/تصنيفات مصروفات لإكمالها بالكامل.

**62. لا توجد طباعة أو تصدير PDF/Excel لأي فاتورة أو تقرير `[عالي]`**
⚠️ **جزئي (Wave 3).** فواتير المبيعات لها PDF فعلي، والداشبورد له تصدير Excel حقيقي. **فواتير المشتريات وإيصالات نقطة البيع (POS) لسه من غير أي تصدير.**

**63. قوائم كتير بتحمّل كل البيانات دفعة واحدة بدون ترقيم صفحات `[متوسط]`**
⚠️ **جزئي.** `contacts` و `sales/invoices` عندهم `limit`/`offset` فعلي. **قائمة `cases` (`cases/api/router.py::list_cases`) لسه من غير أي ترقيم — بترجع كل شيء دفعة واحدة.**

**64. شاشة نقطة البيع غير متجاوبة مع الموبايل/التابلت `[متوسط]`**
❌ **الأرجح لسه موجودة.** `pos/page.tsx` (679 سطر) فيها استخدام واحد بس لـ breakpoints متجاوبة (`sm:`/`md:`/`lg:`) في الصفحة كلها — تصميم شبه ثابت لسطح المكتب.

**65. لا توجد أي عمليات جماعية (Bulk Actions) `[منخفض]`**
❌ **لسه موجودة.** مفيش أي نمط تحديد متعدد + عملية جماعية في صفحات الفواتير أو الحالات أو جهات الاتصال.

**66. أزرار كتير بأيقونة فقط بدون وصف نصي `[منخفض]`**
⚠️ **جزئي/غير متسق.** موجودة أمثلة حقيقية بدون `aria-label` (زرار إظهار/إخفاء كلمة المرور في `register/page.tsx`)، وأمثلة تانية بيها `aria-label` فعلي (`ExpirationAlertsWidget.tsx`). المشكلة غير متسقة عبر النظام، مش عامة بالكامل.

**67. تنسيق التواريخ غير موحّد عبر شاشات النظام `[منخفض]`**
❌ **لسه موجودة (اتأكدت تاني).** `toLocaleDateString("ar-EG")` (7 استخدامات) و `toLocaleDateString("en-GB")` (6 استخدامات) موجودين مع بعض بدون قاعدة موحّدة.

**68. استقبال رسائل واتساب من العملاء موجود لكن غير مستخدم `[منخفض]`**
❌ **لسه موجودة (اتأكدت تاني).** الـ webhook بيستقبل الرسالة وبينشر `WhatsAppMessageReceivedEvent` فعليًا، **لكن مفيش أي listener مشترك في هذا الحدث في أي مكان بالكود** — الرسالة بتتنشر "في الفراغ" وتتجاهل فعليًا.

---

## ملخص عددي نهائي

| الحالة | العدد |
|---|---|
| ✅ تم بالكامل | 22 |
| ⚠️ جزئي | 15 |
| ❌ لسه موجودة | 30 |
| ❓ غير مؤكد بالكامل | 1 |
| **الإجمالي** | **68** |

**أهم 3 حاجات لو هتبدأ منين:**
1. **RBAC (ملاحظة #5)** — لسه أكبر فجوة أمنية حقيقية في النظام كله.
2. **واجهات أمامية مفقودة للباك إند الموجود فعلاً** (ملاحظات #10 أمر البيع، #12 أمر الشراء/الاستلام، #13 تقييم المخزون، #14 تحويلات المخزون) — نمط متكرر: الباك إند جاهز في حالات كتير والفجوة الحقيقية في الفرونت إند بس، يعني إصلاح أسرع نسبيًا من بناء ميزة من الصفر.
3. **الفجوات الجزئية الصغيرة السريعة** (#58 idempotency الدفعات، #62 PDF للمشتريات/POS، #63 ترقيم صفحات الحالات) — كل واحدة فيهم توسعة لنمط موجود بالفعل في مكان تاني بنفس الكود.
