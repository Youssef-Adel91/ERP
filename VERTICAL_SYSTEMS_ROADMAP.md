# خارطة طريق الأنظمة القطاعية (Verticals) — نحو المرجع الأول للـ ERP في الوطن العربي

آخر تحديث: 2026-08-11

## الهدف

الهدف المعلن: نبقى **المرجع الأول** لأنظمة ERP في الوطن العربي. الأنظمة القطاعية (الفنادق، تأجير المركبات، السياحة، التوظيف/تصدير العمالة، واتساب) هي اللي بتفرّق — الكور (محاسبة/مبيعات/مخزون/مشتريات/موارد بشرية) موجود ومتماسك، لكن الأنظمة القطاعية حاليًا **MVP خفيف جدًا** مقارنة بالمنافسين المتخصصين في كل قطاع.

**ملاحظة مهمة**: مفيش أي مستند مواصفات (spec) داخل المشروع بيوصف الأنظمة القطاعية دي بالتفصيل — راجعت كل ملفات الـ Markdown والـ PDFs (`OmniERP_Blueprints_Phase0_Phase8`, `Phase4-8`, `SRS_v2`) ومفيهاش غير إشارات عابرة لواتساب. يعني الأنظمة الخمسة دي اتبنت بدون مواصفات رسمية من الأساس — الملخص تحت مبني على قراءة الكود الفعلي + بحث في السوق عن أفضل الممارسات لكل قطاع.

---

## 1. الفنادق والضيافة (Hospitality)

### الموجود حاليًا
- CRUD كامل للغرف (`/rooms`) + توفر الغرف (`/rooms/availability`)
- Folio: إضافة بنود/رسوم على حجز معين (`/reservations/{case_id}/folio`)
- الحجز نفسه بيعتمد على محرك الـ Cases العام (مش نظام حجوزات مخصص)

### الفجوات (مقارنة بأنظمة PMS الاحترافية)
- مفيش **إدارة تدبير منزلي (Housekeeping)**: حالة الغرفة (نظيفة/متسخة/تحت الصيانة)، تعيين عمال النظافة، تذاكر صيانة
- مفيش **خطط أسعار موسمية (Rate Plans)** — سعر ثابت بس، من غير تسعير حسب الموسم/نوع النزيل/مدة الإقامة
- مفيش **Night Audit** (إقفال يومي تلقائي للفنادق)
- مفيش **Guest Ledger / City Ledger** منفصلين عن الـ Folio العام
- مفيش تكامل **Channel Manager** (Booking.com، Airbnb، إلخ) — مش أولوية دلوقتي لكن مهم لاحقًا
- مفيش **حجوزات جماعية (Group Bookings)**

### الأولويات المقترحة
- **P0**: نظام حجز مخصص (بدل الاعتماد الكامل على Cases العامة) + حالة الغرفة الحية (متاحة/محجوزة/تحت التنظيف)
- **P1**: تدبير منزلي بسيط (حالة الغرفة + تعيين مهام) + خطط أسعار موسمية
- **P2**: Night Audit، Channel Manager، حجوزات جماعية

---

## 2. تأجير المركبات (Rental)

### الموجود حاليًا
- CRUD كامل للأسطول (`/fleet`) + توفر المركبات
- فحص المركبة عند الاستلام/التسليم مع مقارنة الفروقات (`/inspections/diff`) — ده فعليًا **أقوى جزء** في الأنظمة القطاعية الحالية

### الفجوات
- مفيش **عقود إيجار رقمية (Digital Contracts)** — العقد نفسه مش موجود كمستند/entity منفصل
- مفيش **فوترة أضرار تلقائية** مربوطة بنتيجة الفحص (الـ diff موجود بس مفيش ربط تلقائي بفاتورة)
- مفيش **جدولة صيانة** للمركبات (كم/تاريخ آخر صيانة، تنبيه الصيانة القادمة)
- مفيش تتبع **تراخيص/تأمين/مخالفات مرورية** لكل مركبة
- مفيش تكامل GPS/تتبع الموقع (مش أولوية للسوق المحلي دلوقتي)

### الأولويات المقترحة
- **P0**: عقد إيجار كمستند رسمي (PDF) + ربط نتيجة الفحص بفاتورة أضرار تلقائية
- **P1**: جدولة الصيانة + تتبع التأمين/الترخيص لكل مركبة
- **P2**: تكامل GPS، تسعير ديناميكي حسب الموسم/الطلب

---

## 3. السياحة والسفر (Travel)

### الموجود حاليًا
- **أضعف الأنظمة الخمسة**: مفيش غير endpoint واحد لحساب الماليات الخاصة بحجز (`/bookings/{case_id}/financials`) + bootstrap
- الحجز نفسه، الباقات، الفنادق/الرحلات المرتبطة — كل ده بيعتمد بالكامل على محرك الـ Cases العام من غير أي structure سياحي مخصص

### الفجوات
- مفيش **باقات سياحية (Packages)** كـ entity — أسعار، مكونات (فندق+نقل+جولات)، إتاحة
- مفيش **بناء برنامج رحلة (Itinerary Builder)** يوم بيوم
- مفيش تتبع **تأشيرات (Visa Processing)** كخطوة منفصلة قابلة للتتبع
- مفيش **صافي السعر مقابل هامش الربح (Net Rate vs Markup)** لكل مورد — ده أساسي في أي ERP سياحي حقيقي
- مفيش **تسوية BSP** (لو هيبقى فيه تذاكر طيران) أو عمولات موردين

### الأولويات المقترحة
- **P0**: نموذج باقة سياحية (Package) + برنامج رحلة (Itinerary) كـ entities حقيقية بدل الاعتماد الكامل على Case عامة
- **P0**: صافي السعر/هامش الربح لكل عنصر في الباقة (مرتبط بالفعل بـ `Vendor`/`VendorRateCard` الموجودين، محتاجين استخدام فعلي)
- **P1**: تتبع تأشيرات كخطوة Case مخصصة (case_type محدد بمراحل تأشيرة)
- **P2**: تسوية BSP، عمولات معقدة

---

## 4. التوظيف وتصدير العمالة (Recruitment)

### الموجود حاليًا
- **أنضج الأنظمة الخمسة**: طلبات توظيف (Job Orders) CRUD كامل، مطابقة مرشحين تلقائية (`/candidates/match`)، استيراد مرشحين بالجملة
- مبني فوق محرك Cases (مناسب هنا لأن التوظيف بطبيعته عملية بمراحل)

### الفجوات
- مفيش **جدولة مقابلات** (Interview Scheduling) كخطوة مخصصة
- مفيش **خطابات عرض عمل (Offer Letters)** كمستندات مولّدة
- مفيش **تتبع عمولة/رسوم التوظيف** لكل عملية تنسيب (placement fee)
- تتبع التأشيرة/التصريح لعمال التصدير — موجود جزئيًا عبر `CaseContact.meta` (شفناه في تنبيهات انتهاء الصلاحية) بس مش مبني كواجهة مخصصة
- مفيش **بوابة عميل (Client Portal)** يشوف بيها صاحب العمل حالة طلبه

### الأولويات المقترحة
- **P0**: جدولة مقابلات + تتبع عمولة/رسوم التنسيب
- **P1**: خطابات عرض عمل مولّدة، واجهة مخصصة لتتبع التأشيرة (مبنية على الـ alerts الموجودة بالفعل)
- **P2**: بوابة عميل خارجية

---

## 5. تكامل واتساب (WhatsApp)

### الموجود حاليًا
- إعداد رقم الأعمال (`/config`) + استقبال Webhooks + إرسال رسائل Template فقط

### الفجوات
- مفيش **صندوق محادثات (Inbox)** داخل الداشبورد لعرض/الرد على رسائل العملاء
- مفيش **إرسال جماعي/حملات (Broadcast)** لعروض أو تذكيرات
- مفيش **الميزة المذكورة في مستندات المشروع الأصلية نفسها**: تحويل صورة/رسالة واتساب لأمر بيع مسودة تلقائيًا (مذكورة في `OmniERP_Blueprints_Phase0_Phase8.pdf` كميزة Copilot لكن مش موجودة في الكود)

### الأولويات المقترحة
- **P0**: صندوق محادثات بسيط داخل الداشبورد (عرض + رد يدوي)
- **P1**: تنفيذ ميزة "تحويل رسالة واتساب لأمر بيع" المذكورة أصلًا في المستندات
- **P2**: حملات جماعية

---

## قبل ما نبدأ التنفيذ

كل نظام من دول فعليًا مشروع لوحده — تنفيذ كل حاجة في القائمة دي شغل أسابيع مش جلسة واحدة. المقترح: نرتب الأولويات مع بعض (مثلاً نبدأ بكل بنود الـ P0 عبر الخمس أنظمة الأول، أو نركز على نظام واحد ونخلّصه بالكامل قبل ما ننتقل للتاني) وبعدين أبدأ التنفيذ فعليًا بالترتيب اللي تحدده.

سؤال محتاج إجابتك عليه قبل ما أبدأ بناء أي حاجة: **إيه الترتيب اللي تحبه؟** كل الـ P0 الأول عبر كل الأنظمة، ولا نظام واحد كامل في كل مرة؟
