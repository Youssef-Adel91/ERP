# PROJECT_STATUS — OmniERP (Nexus ERP)

آخر تحديث: 2026-08-09 (جلسة إصلاح الباجات — المرحلة 3 من برومبت التسليم الكامل)

---

## ملخص الجلسة الحالية

اشتغلت على المرحلة (3) من برومبت التسليم الكامل: إصلاح المشاكل المعروفة بالأولوية العالية والمتوسطة. **لم أبدأ بعد في المرحلة (4) تصميم الواجهة ولا المرحلة (5) إعادة هيكلة الباقات** — دي محتاجة موافقة/قرارات منك الأول (تفاصيل في آخر الملف).

### بيئة العمل المستخدمة
اشتغلت من بيئة Linux sandbox معزولة (مش الويندوز بتاعك)، ليها وصول لملفات المشروع مباشرة لكن **من غير** الـ `.venv` بتاعك (اللي هو Python 3.11 على ويندوز) ومن غير تشغيل فعلي ضد Neon الحقيقية. يعني:
- ✅ كل التعديلات البرمجية اتعملت وتأكدت إنها صحيحة **نحويًا/ statically** (syntax check للباكاند عبر `ast.parse`، و`tsc --noEmit` كامل للفرونت إند نجح من غير أخطاء).
- ⚠️ **لسه محتاج تأكيد حي (end-to-end) منك**: شغّل السيرفرين زي المعتاد (`uvicorn` + `npm run dev`) وجرّب السيناريوهات المذكورة تحت "خطوات التحقق المطلوبة منك" تحت.

---

## المشاكل اللي اتصلحت (المرحلة 3)

### 1. باج RTL في فورم "إضافة صنف جديد" (المخزون) — ❌ لم يكن هناك باج فعليًا
فحصت `frontend/src/app/dashboard/inventory/page.tsx`. الفورم بيستخدم `react-hook-form` مع `register("sku")` و `register("egs_code")` — الربط بين كل input وقيمته في الـ state بيتم بالاسم (name-based binding) مش بترتيب الـ DOM، فمفيش أي طريقة تقنية يحصل بيها تبديل قيم بين الحقلين حتى لو الترتيب البصري في RTL اتغيّر. كمان ترتيب الحقلين في الشبكة (SKU أولًا/يمين، ETA تانيًا/شمال) بيطابق فعليًا اتجاه القراءة العربي الطبيعي.
**الخلاصة**: يبدو إن ده false positive من أداة الفحص، أو كان اتصلح في جلسة سابقة غير موثقة. **محتاج منك تتأكد فعليًا** بفتح الفورم وتجربة إدخال قيمتين مختلفتين والتأكد إنهم بيوصلوا صح.

### 2. تسريب أخطاء SQL خام للمستخدم — ✅ اتصلح
في `backend/app/main.py`:
- أضفت `exception_handler(SQLAlchemyError)` و `exception_handler(Exception)` بعد تسجيل الـ middlewares.
- أي استثناء غير متوقع (SQL error، أو أي حاجة تانية) بيتسجل كامل في الـ logs (مع traceback) بس بيرجع للمستخدم رسالة عامة: `"حدث خطأ غير متوقع. حاول مرة أخرى أو تواصل مع الدعم الفني."` مع status 500.
- الـ handler بيتأكد إنه مش بيمسك `HTTPException`/`StarletteHTTPException`/`RequestValidationError` (اللي هي رسائل مقصودة زي "لا يمكنك إغلاق وردية كاشير آخر") — لو حصلت غلطة وحد فيهم وصل للـ handler العام، بيعمل `raise` تاني بدل ما يبلعها.

**للتحقق**: كرر سيناريو `costingmethod` (أو أي خطأ SQL تاني) وشوف إن الرسالة اللي بترجع للمستخدم عامة، والتفاصيل الكاملة موجودة بس في الـ terminal بتاع uvicorn.

### 3. نمط رسائل الخطأ العامة في صفحات الداشبورد — ✅ اتصلح
أضفت دالة `getApiErrorMessage(error, fallback)` في `frontend/src/lib/api-client.ts` بتفرّق بين:
- خطأ شبكة حقيقي (`ERR_NETWORK` / مفيش response) → "تعذر الاتصال بالخادم، تأكد من اتصالك بالإنترنت."
- 401 → "انتهت صلاحية جلستك..." (ملحوظة: الـ interceptor الموجود أصلًا بيعالج الـ 401 بمحاولة refresh token وبعدين redirect لـ `/login` تلقائيًا، فالرسالة دي نادرًا ما تظهر فعليًا — هي شبكة أمان بس).
- 403 → "ليس لديك صلاحية..."
- 404 → "العنصر المطلوب غير موجود."
- 500+ → "حدث خطأ في الخادم، حاول مرة أخرى."
- أي 4xx تاني → بيفضّل رسالة `detail` الراجعة من السيرفر لو موجودة (رسائل validation مقصودة)، وإلا الـ fallback المحدد.

طبّقتها على الصفحات التسعة اللي كانت بتستخدم النمط القديم: `accounting`, `cases`, `contacts`, `finance`, `hr`, `imports`, `news`, `settlements`, `team`. كل صفحة دلوقتي بتستدعي `getApiErrorMessage(err, "<رسالة مخصصة للصفحة>")` بدل الرسالة الثابتة القديمة.
**ملحوظة**: صفحة `dashboard/page.tsx` (لوحة القيادة الرئيسية) بتستخدم `react-query` بنمط مختلف (`isError` booleans) مش `catch{}` مباشر — سيبتها زي ما هي لأنها مش من ضمن الصفحات المذكورة صراحةً في الطلب، لكن ينفع تتعمل نفس المعالجة لاحقًا لو حبيت.

**للتحقق**: `tsc --noEmit` شغال نظيف بدون أخطاء على المشروع كله بعد التعديل.

### 4. عدم اتساق لغوي — أسماء الحسابات الافتراضية بالإنجليزي — ✅ اتصلح جزئيًا
لقيت إن الموديل `Account` أصلًا فيه حقل `name_ar` جاهز (وكود الـ seeding في `database.py` كان بيقرأه أصلًا عبر `acct_data.get("name_ar")`) — بس الـ `DEFAULT_ACCOUNTS` list في `backend/app/modules/accounting/models/core.py` مكنش فيها قيم `name_ar` خالص. أضفتها لكل الحسابات الثمانية (نقدية، عملاء، مخزون، موردون، حقوق ملكية، إيرادات مبيعات، تكلفة بضاعة مباعة، مصروفات تشغيلية).
وفي الفرونت `frontend/src/app/dashboard/accounting/page.tsx` عدّلت عرض اسم الحساب من `{a.name}` لـ `{a.name_ar || a.name}`.

**ملحوظات مهمة**:
- ده بيأثر بس على **تينانتس جداد** هيتسجلوا من دلوقتي (لأن الـ seeding بيحصل وقت provisioning). التينانتس الموجودين فعلاً مش هيتغيروا تلقائيًا (لو عايز، محتاج migration/script إضافي يعمل UPDATE على الحسابات الموجودة بناءً على الـ code).
- في أماكن تانية في الكود (`app/modules/accounting/events.py`, `app/modules/accounting/reports/trial_balance.py`) بيتحط اسم الحساب بالإنجليزي هارد-كودد في القيود المحاسبية التلقائية وتقرير ميزان المراجعة — دي أعمق من مجرد "seed data" (كانت برضو خارج نطاق الطلب المحدد) ومحتاجة شغل إضافي لو عايز الاتساق يبقى شامل 100%.

### 5. (أولوية متوسطة) فصل صلاحيات في الموديولات القطاعية الأربعة — ✅ مفيش حاجة محتاجة تتعمل
فحصت `app/plugins/{hospitality,rental,recruitment,travel}` — لقيت إن `require_plugin()` (اللي كل الراوترات دي متعلّقة بيه في `main.py`) أصلًا بيعتمد داخليًا على `CurrentUser` (`app/core/dependencies/plugin_gate.py`)، يعني أي طلب لأي endpoint في الأربع موديولات دي لازم يعدي أولًا على resolve مستخدم حقيقي ونشط من قاعدة البيانات قبل ما يتحقق حتى من تفعيل الـ plugin. المشكلة الموصوفة (أي مستخدم مسجل دخول حتى لو كاشير بسيط يقدر يوصل لصفحات المدير) مش موجودة فعليًا حاليًا لأن **النظام فيه بس role اتنين**: `OWNER` و `ADMIN` — الاتنين أصلًا صلاحيات عالية، مفيش "موظف/كاشير" كـ role منفصل. لو عايز تفرقة أدق (مدير مقابل موظف) لازم الأول تتقرر: هل نضيف roles جديدة؟ وإيه بالظبط اللي المفروض كل role يقدر يعمله؟ ده قرار منتج/عمل مش مجرد كود.

---

## خطوات التحقق المطلوبة منك (على جهازك، مش هنا)

شغّل النظام زي المعتاد وجرّب:
1. **إضافة صنف جديد بالمخزون** — تأكد إن SKU وكود ETA بيتسجلوا صح (متوقع: تمام أصلًا، كان false positive).
2. **تعمّد خطأ سيرفر** (مثلاً جرب تكرر إدخال كود موجود، أو أي عملية كانت بتطلع خطأ SQL قبل كده) وشوف إن اللي بيظهر للمستخدم رسالة عامة بس، مش تفاصيل SQL.
3. **افصل الإنترنت لحظيًا** وجرب تحميل صفحة زي `contacts` أو `hr` — يفترض تشوف "تعذر الاتصال بالخادم، تأكد من اتصالك بالإنترنت" بدل الرسالة القديمة العامة.
4. **سجّل تينانت جديد بالكامل** وافتح صفحة "الحسابات والقيود" (`accounting`) — شجرة الحسابات المفروض تظهر بأسماء عربي ("النقدية وما يعادلها" بدل "Cash & Cash Equivalents").
5. تأكد عام إن حل مشكلة الـ 503 المتفرقة (المذكورة في تسليم سابق بخصوص الـ pooled connection) لسه ثابت.

بلّغني بنتيجة التجربة دي، خصوصًا لو ظهر أي خطأ TypeScript/Python وقت التشغيل الفعلي مش قدرت أمسكه من غير سيرفر شغال.

---

## الخطوات الجاية (لسه محتاجة قرار/موافقة منك)

### المرحلة 4 — تصميم حديث للواجهة

#### الجولة الأولى (نموذج حر، اتصحّح لاحقًا)
عدّلت `dashboard/page.tsx` كنموذج تجريبي حر (كروت `rounded-2xl`، ظلال وتحركات إضافية مني). ده كان **قبل** ما ألاقي فولدر Stitch الجاهز.

#### الجولة الثانية — تطبيق نظام Stitch الرسمي (direction_6) — ✅ خلصت للصفحة التجريبية، في انتظار موافقتك على التعميم
المستخدم وفّر فولدر `stitch_egyptian_erp_design_system_generator/` فيه 10 مرجعيات تصميم معتمدة (`direction_6_*`, كل واحدة `code.html` + `screen.png`). خطوات التحقق والتنفيذ:

1. **قارنت توكنز الـ 10 ملفات ببعض برمجيًا (مش بالعين)** — الألوان (47 توكن)، الـ borderRadius، الـ spacing، الـ fontFamily/fontSize كلها **متطابقة حرفيًا** بين الـ 10 ملفات.
2. **قارنتها بـ `frontend/src/app/globals.css`** — طابقت **100%** بنفس القيم الست عشرية. يعني التطبيق الحالي أصلًا مبني على نفس نظام Stitch ده (أو نسخة سابقة منه) — مفيش داعي لأي تغيير في الألوان أو الخطوط أو الـ spacing.
3. **الفرق الوحيد اللي لقيته**: `frontend/tailwind.config.ts` فيه إضافات زيادة عن Stitch الرسمي (`rounded-2xl`, و`success`/`warning` كألوان status) — دي مش تعارض (Stitch نفسه بيعيد استخدام `secondary`/`error` كمؤشرات، إحنا ضفنا ألوان status مخصصة، وده مقبول)، بس **صححت استخدام `rounded-2xl`** في نسخة الداشبورد التجريبية اللي عملتها في الجولة الأولى، ورجّعتها `rounded-xl` بالظبط زي المرجع (الـ radius الرسمي بيوقف عند `xl` = 12px).
4. **عملت `frontend/DESIGN_TOKENS.md` جديد** (المرجع القديم في جذر المشروع `../DESIGN_TOKENS.md` طلعت بتاعة نظام تصميم قديم تاني "Trust Core" — ink/accent/paper — بتاع `frontend_old/` بس، مش مطبّق في `frontend/` الحالي خالص. سيبته موجود وأشرت إنه قديم بدل ما أمسحه من غير سؤالك).
5. **عملت مكونات React مشتركة** تحت `frontend/src/components/ui/`: `Card` + `CardHeader`, `Badge` + `StatusBadge`, `StatCard` (كارت الـ KPI بالظبط زي `direction_6_data_visual_dashboard`), `EmptyState`. دول مستخرجين من الأنماط المتكررة في الـ 10 ملفات، والهدف إن باقي الصفحات تستخدمهم بدل تكرار الـ classNames.
6. **أعدت بناء `dashboard/page.tsx`** باستخدام المكونات دي، مطابقة لبنية `direction_6_data_visual_dashboard/code.html` بالظبط (نفس ترتيب الهيدر، نفس شكل كروت الـ KPI، نفس شكل الجدول والبادجات).

**قرار تعمّدت اتخاذه (محتاج رأيك)**: مرجعية الداشبورد فيها بادج "trend %" (زي "+12%") فوق كل كارت KPI، ورسمين بيانيين (خط إيرادات سنوي + دائرة توزيع مصروفات) بأرقام وهمية مرسومة بالـ SVG. **متعمدتش أضيفهم** لأن مفيش endpoint حقيقي في الباكاند يديني نسبة تغيّر عن الشهر اللي فات أو تفصيل توزيع المصروفات — عرض أرقام مختلقة على داشبورد مالي حقيقي هيكون مضلل. لو عايز الشكل ده فعليًا، محتاجين الأول نضيف endpoints جديدة في الباكاند تحسب المقارنات دي من بيانات حقيقية.

**خريطة ربط المرجعيات العشرة بالصفحات الحقيقية**:

| مرجعية Stitch | الصفحة الحقيقية | الحالة |
|---|---|---|
| `direction_6_login` | `(auth)/login/page.tsx` | موجودة، لسه محتاجة تطبيق التصميم |
| `direction_6_data_visual_dashboard` | `dashboard/page.tsx` | ✅ اتعملت |
| `direction_6_contacts_directory` | `dashboard/contacts/page.tsx` | موجودة، لسه محتاجة تطبيق |
| `direction_6_contact_detail` | **مفيش صفحة تفاصيل جهة اتصال (`contacts/[id]`) أصلًا** | ⚠️ فجوة — صفحة جديدة كاملة، مش مجرد إعادة تصميم. متبنيتهاش من غير ما تأكد |
| `direction_6_financial_core` (نظرة عامة على السيولة) | الأقرب `dashboard/finance/page.tsx` (الشيكات/السيولة) | موجودة، لسه محتاجة تطبيق |
| `direction_6_transactions_ledger` (دفتر الأستاذ العام) | تبويب "القيود" داخل `dashboard/accounting/page.tsx` | موجودة، لسه محتاجة تطبيق |
| `direction_6_plugins_marketplace` | `dashboard/marketplace/page.tsx` | موجودة، لسه محتاجة تطبيق |
| `direction_6_reports_grid` (مستودع تقارير) | **مفيش صفحة تقارير أصلًا** | ⚠️ فجوة — صفحة جديدة كاملة. متبنيتهاش من غير ما تأكد (زي ما طلبت بالظبط) |
| `direction_6_settings` | `dashboard/settings/page.tsx` | موجودة، لسه محتاجة تطبيق |
| `direction_6_compliance` (امتثال ضريبي) | الأقرب `dashboard/eta/page.tsx` | موجودة، لسه محتاجة تطبيق |

**باقي الصفحات (مالهاش مرجعية Stitch مباشرة)**: inventory, sales, purchases, hr, cases, imports, team, news, billing, approvals, pos, shipping, vendors, settlements, travel, hospitality, rental, recruitment, trust — دي أصلًا بتستخدم نفس التوكنز والـ layout المشترك (الـ sidebar/header في `dashboard/layout.tsx` مطابق لـ Stitch فعلًا من غير أي تعديل)، فمحتاجة بس تحديث الكروت/الجداول جوه كل صفحة تستخدم المكونات المشتركة الجديدة.

**فولدرات التصميم القديم**: `frontend_old/` (2.2MB) و`src_old/` (340KB) في جذر المشروع — تأكدت إنهم **معزولين تمامًا**، مفيش أي ملف في `frontend/` الحالي بيستورد منهم أو بيشير ليهم. مرشحين آمنين للحذف، **بس ما حذفتهمش** — محتاج تأكيدك الصريح الأول.

**التحقق**: `tsc --noEmit` شغال 3 مرات بعد كل تعديل، نضيف كل مرة. `next build` الكامل timeout في الـ sandbox بتاعي (43 ثانية مش كفاية له يخلص) — مش نتيجة فشل، بس مقدرتش أأكدها لحد ما تشغّلها عندك. `eslint` فشل يشتغل من غير ما يوصل لكودي أصلًا (مشكلة في resolve الأداة نفسها جوه الـ sandbox، مش في الكود).

**⏸️ متوقف هنا في انتظارك على قرارين**:
1. هل أكمل أطبّق نفس الأسلوب على باقي الصفحات التسعة اللي ليها مرجعية مباشرة، وبعدين الباقي؟
2. تأكيد: أحذف `frontend_old/` و`src_old/` بالكامل؟ (لو أيوه، هستنى لحد ما تتأكد إن التصميم الجديد شغال 100% في `frontend/` الأول، مش هحذفهم دلوقتي حتى لو قلت أيوه، عشان يفضل فيه نسخة احتياطية لحد ما نخلص).

### المرحلة 5 — إعادة الهيكلة لنموذج "الباقات الكاملة المنفصلة"
لسه ما بدأتش فيها. لاحظت حاجة مهمة: فيه أصلًا `packages_router` موجود في `app/modules/system/api_plugins.py` ومربوط في `main.py` (البند 25 "Plugin Marketplace") — يعني فيه أساس جزئي جاهز أكتر مما كان متوقع في البرومبت الأصلي، محتاج فحص أعمق قبل ما أبني حاجة جديدة. المرحلة دي فيها قرارات عمل (سعر كل باقة، مميزات كل باقة بالظبط، هل نمسح بيانات التجربة الحالية) لازم تتاخد منك الأول.
