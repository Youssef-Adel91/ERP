# alembic.ini — Alembic Configuration
# NOTE: The sqlalchemy.url is intentionally left as a placeholder.
#       It is programmatically overridden in alembic/env.py using the
#       value from Pydantic Settings (reading from .env).

[public]
# Path to migration scripts for the public schema
script_location = alembic/public

# Template for new migration file names
file_template = %%(rev)s_%%(slug)s

# sys.path modification for imports
prepend_sys_path = .

[tenant]
# Path to migration scripts for the tenant schemas
script_location = alembic/tenant

# Template for new migration file names
file_template = %%(rev)s_%%(slug)s

# sys.path modification for imports
prepend_sys_path = .

# Timezone to use in migration file creation
timezone = UTC

# max length of characters to apply to the
# "slug" field
truncate_slug_length = 40

# set to 'true' to run the environment during
# the 'revision' command, regardless of autogenerate
# revision_environment = false

# set to 'true' to allow .pyc and .pyo files without
# a source .py file to be detected as revisions in the
# versions/ directory
# sourceless = false

# version location specification; This defaults
# to alembic/versions.  When using multiple version
# directories, initial revisions must be specified with --version-path.
# version_locations = %(here)s/bar:%(here)s/bat:alembic/versions

# version path separator; As mentioned above, this is the character used to split
# version_locations. The default within new alembic.ini files is "os", which uses
# os.pathsep. Note that this key is only used when you specify
# "version_locations" as a comma or semicolon separated list
# Set this to "space" if you want to use a space as the separator
version_path_separator = os  # Use os.pathsep. Default configuration used for new projects.

# the output encoding used when revision files
# are written from script.py.mako
# output_encoding = utf-8

sqlalchemy.url = postgresql+asyncpg://placeholder/placeholder


[post_write_hooks]
# post_write_hooks defines scripts or Python functions that are run
# on newly generated revision scripts.  See the documentation for further
# detail and examples

# format using "black" - use the console_scripts runner, against the "black" entrypoint
# hooks = black
# black.type = console_scripts
# black.entrypoint = black
# black.options = -l 79 REVISION_SCRIPT_FILENAME

# Logging configuration
[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
